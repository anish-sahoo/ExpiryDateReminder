<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the system splash needs an XML theme; everything after it is Compose and
         Material You. The pre-2.0 app faked a splash with a 2000ms Handler delay. -->
    <style name="Theme.Edr.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:attr/colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Edr</item>
    </style>

    <!-- Must descend from Theme.AppCompat: MainActivity is an AppCompatActivity so that
         AppCompatDelegate.setApplicationLocales can drive the in-app language picker. -->
    <style name="Theme.Edr" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
